#define x aasd
#include <stdio.h>
#define x aasd
int an, bsa, dds;
float sds;
char sdas;
int testfun(int lsk, int s, int sd){
    int a1, b2, c3;
}
int main(int amm, char bsa, float ssl){
    int a, b, c;//test
    char ap, sp;
float s;//sadalk

    //int a;
    //else
    /*
    int bm;
    */
    //s-
    //int a;
    int bm;
    // 1 = a - 3 +. 2;
    a = b++ + (1 -    3) - 2;
    bm = 1 + (testfun(a, b, testfun(testfun(a,b,c),b,c)) - 1) * 3 + 2;





if(ap == sp){
        if(2){
            testfun(a,b,c);
        }
    }
    else{
        testfun(a,b,c);
    }
    while(1){
        while(1){
            continue;
        }
        testfun(a,b,c);
        continue;
        ;}while(1) return 0;
    int i;
    if(1) if(1) if(1) a = 1; else if(1) b = 2; else c = 3;
    for(i = 1; i<= 2; i+=1) testfun(a,b,c);
    return 0+3;
}